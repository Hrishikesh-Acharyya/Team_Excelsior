# ⚙️ Django Settings
DEBUG=True
SECRET_KEY=your-django-secret-key

# 🌐 Allowed Hosts
ALLOWED_HOSTS=localhost,127.0.0.1
HOST_NAME = 'Host_name'

# 🔐 JWT Token Settings (Optional overrides)
ACCESS_TOKEN_LIFETIME=5  # in minutes
REFRESH_TOKEN_LIFETIME=1  # in days

# 🗄️ PostgreSQL Database Settings
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# 🤖 Together.ai LLaMA API Key
TOGETHER_AI_API_KEY=your_together_api_key
TOGETHER_AI_API_URL =  'https://api.together.xyz/v1/chat/completions'

# 🎙️ Whisper (no key needed, but path may be helpful)
WHISPER_MODEL=base  # can be: tiny, base, small, medium, large
WHISPER_LANGUAGE=hi  # Optional default language, e.g., 'hi' for Hindi

# ⚙️ CORS (if frontend is separate)
CORS_ALLOWED_ORIGINS=http://localhost:3000